function setup() {
    createCanvas(600, 600);
    textAlign(CENTER, CENTER);
    textSize(32);
}

function draw() {
    background(57, 225, 20); // Set the background color to green
    fill(0); // Set text color to black
    text("Hello, World!", width / 2, height / 2); // Display text in the center
}